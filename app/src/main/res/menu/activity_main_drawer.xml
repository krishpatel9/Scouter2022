<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item android:title="Data">
        <menu>
            <item
                android:id="@+id/nav_matches"
                android:icon="@drawable/robot"
                android:title="View All Matches" />
        </menu>
    </item>
    <item android:title="Generate">
        <menu>
            <item
                android:id="@+id/nav_email"
                android:enabled="false"
                android:icon="@drawable/ic_menu_send"
                android:title="Email"
                android:visible="false" />
            <item
                android:id="@+id/nav_code"
                android:icon="@drawable/qrcode"
                android:title="Hex Code/QR Code" />
        </menu>
    </item>

    <item android:title="Preferences">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/settings"
                android:title="Settings" />
            <item
                android:id="@+id/nav_software"
                android:icon="@drawable/ic_storage_black_24dp"
                android:title="Add'l Software" />
            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/about"
                android:title="About" />
        </menu>
    </item>

</menu>
